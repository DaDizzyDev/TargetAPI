const Discord = require('discord.js');

module.exports = {
    id: 'help', // Enter command here.
    aliases: ['help'], // Alternatives for above.
    channels: 'all', // Keep as "all".
    exec: (call) => {
        // ENTER CODE HERE //
    }
};